[pytest]
testpaths = tests
# The modules under test live in this directory, not a package, so put it on
# the path explicitly — otherwise the suite only passes when invoked as
# `python -m pytest`, which happens to prepend the cwd.
pythonpath = .
addopts = -q
